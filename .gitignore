# 檔案名稱 .gitignore

# 忽略 config 目錄下的檔案
config/firebaseConfig.js
config/serviceAccountKey.json
function/users.js

# 忽略所有 node_modules 目錄下的檔案
/node_modules/*

# 當然你要忽略自己也可以，只是通常不會這麼做
# .gitignore
package-lock.json
